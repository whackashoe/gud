CMAKE_MINIMUM_REQUIRED (VERSION 2.6)
PROJECT (helloworld)
FIND_PACKAGE (Boost REQUIRED)

SET (SOURCES
	main.cpp)
ADD_EXECUTABLE (helloworld
	${SOURCES})
TARGET_LINK_LIBRARIES (helloworld
	web)
TARGET_LINK_LIBRARIES( helloworld boost_regex )